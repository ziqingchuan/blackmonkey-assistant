{
  "rewrites": [
    { "source": "/:path*",
      "destination": "/index.html"
    }
  ],

  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "http://localhost:8000/api:path*",
      "methods": ["GET", "POST", "PUT", "DELETE"]
    }
  ]
}